<%= form_with(model: categorie_parametre) do |form| %>
  <% if categorie_parametre.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(categorie_parametre.errors.count, "error") %> prohibited this categorie_parametre from being saved:</h2>

      <ul>
        <% categorie_parametre.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

      <div class="input-group input-group-sm my-1">
        <%= form.label :game_id, class: "input-group-text label-colored"  %>
        <%= form.collection_select :game_id, Game.all, :id, :nom,  class: "form-select input-colored" %>
      </div>
      <div class="input-group input-group-sm my-1">
        <%= form.label :categorie, class: "input-group-text label-colored"  %>
        <%= form.text_field :val_categorie,  class: "form-control input-colored" %>
      </div>

  <div>
    <%= custom_submit_button("valider") %> 
  </div>
<% end %>


