
<div class="container-fluid d-flex m-1">
    <strong class="mx-1">Secondaire</strong>

    <%# if @problemes_secondaires.present? %>

      <% @problems_secondaires.each do |problem_secondaire| %>
          <%= button_to problem_secondaire.nom, 
              display_correctif_setups_path(problem_secondaire_id: problem_secondaire.id), 
                  class: "btn btn-secondary btn-pb2 mx-1",
                  data: { controller: "button-setuptool", action: "click->button-setuptool#handleClickPb2" } %>
      <% end %>
    
    <%# end %>
    

</div>

    <div id="partial-correctifs-container">
      <%# recoit l'update turbo stream avec une partial donnée %>
    </div>
