<%= form_with(model: comportement_base_setup) do |form| %>
  <% if comportement_base_setup.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(comportement_base_setup.errors.count, "error") %> prohibited this comportement_base_setup from being saved:</h2>

      <ul>
        <% comportement_base_setup.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="input-group input-group-sm my-2">
    <%= form.label :sens, class: "input-group-text label-colored" %>
    <%= form.select :sens, [['monter', 'monter'], ['descendre', 'descendre']], prompt: 'Select Sens', class: "form-control input-colored" %>
  </div>
  
  <div class="input-group input-group-sm my-2">
    <%= form.label :base_setup_id, class: "input-group-text label-colored" %>
    <%= form.collection_select :base_setup_id, BaseSetup.all, :id, :parametre, class: "form-control input-colored" %>
  </div>

  <div class="input-group input-group-sm my-2">
    <%= form.label :comportement_id, class: "input-group-text label-colored" %>
    <%= form.collection_select :comportement_id, Comportement.all, :id, :nom, class: "form-control input-colored" %>
  </div>

  <%= custom_submit_button("Valider") %>

<% end %>
