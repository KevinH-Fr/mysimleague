
<% if current_user %>
    <div class="flex-element text-light p-2">

        <%= link_to  "", user_path(current_user), 
                 data: {turbo: "false"}, class:"fa-solid fa-id-card btn btn-warning" %>

        <%= current_user.nom %> 
        <%= button_to "", destroy_user_session_path, method: :delete, 
            data: {turbo: "false"},
                class: "fa-solid fa-right-from-bracket btn btn-danger"  %>

    </div>
<% else %>
    <div class="flex-element p-2">
        <%= link_to '', new_user_session_path, 
                class: "fa-solid fa-user-lock btn btn-warning",
                'aria-label' => "Sign In"%>

        <%#= link_to "", steps_compte_step1_path, 
        data: {turbo: "false"},
                class: "fa-solid fa-user-plus btn btn-primary",
                'aria-label' => "Sign Up" %>

        <%= link_to  "", new_user_registration_path,
                class: "fa-solid fa-user-plus btn btn-primary",
                'aria-label' => "Sign Up"  %>
    </div>
<% end %>
        